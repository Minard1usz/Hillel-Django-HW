# 📝 Список промптів, використаних під час виконання лабораторної роботи (AI_PROMPTS.md)
У цьому файлі наведено перелік ключових запитів (промптів), які надсилалися штучному інтелекту для рефакторингу коду, налаштування асинхронності, написання тестів та документування проєкту.
---
## 🚀 1. Рефакторинг та оптимізація (Асинхронність та БД)
* **Промпт для переведення `BookListView` на асинхронні рейки та кешування:**
    > "Допоможи переписати Django Class-Based View `BookListView` на асинхронний режим. Мені потрібно додати асинхронний метод `get`, оптимізувати SQL-запити через `select_related('category')`, а також реалізувати кешування списку категорій у Redis за допомогою `cache.get_or_set`."
* **Промпт для оптимізації головної сторінки (`store`) через паралельні запити:**
    > "У мене є в'юха `store`, яка робить 5 різних запитів до БД. Як мені зробити її асинхронною та запустити ці запити паралельно за допомогою `asyncio.gather`, щоб пришвидшити завантаження?"
* **Промпт для захисту від Race Conditions (формлення замовлення):**
    > "Як захистити процес створення замовлення в Django від стану гонитви (Race Conditions)? Допоможи написати синхронну логіку всередині `transaction.atomic()`, яка використовує `select_for_update()` для блокування залишків книг на складі."
---
## 🛠️ 2. Виправлення архітектурних помилок та багів
* **Промпт для розділення асинхронного контексту та синхронних сесій:**
    > "Моя асинхронна в'юха створення замовлення ламається, бо Django сесії (Cart) та форми працюють синхронно. Допоможи розділити логіку на окремі синхронні функції-обробники `_handle_order_get` та `_handle_order_post` і викликати їх через `sync_to_async`."
* **Промпт для фінансового округлення в Stripe:**
    > "При передачі ціни в Stripe виникають помилки з плаваючою крапкою (float). Напиши функцію `_to_stripe_amount`, яка конвертує Decimal у копійки (int) за допомогою фінансового округлення ROUND_HALF_UP."
---
## 🧪 3. Написання юніт-тестів (Pytest)
* **Промпт для генерації тестів моделей:**
    > "Згенеруй юніт-тести для Django-моделей `Category` та `Book` за допомогою `pytest`. Потрібно перевірити методи `__str__`, verbose_names у Meta, а також валідацію полів."
* **Промпт для вирішення проблеми з IntegrityError (PostgreSQL check constraint):**
    > "Мій тест для валідації від'ємного значення `stock` падає з `django.db.utils.IntegrityError: violates check constraint`. Як змусити pytest-django коректно перехопити цю помилку бази даних за допомогою `@pytest.mark.django_db(transaction=True)` або валідації в пам'яті розробника?"
---
## 📖 4. Автоматична генерація документації (Docstrings)
* **Промпт для документування за стандартом PEP 257:**
    > "Згенеруй професійні docstrings для моїх Django views відповідно до стандарту PEP 257. Опис повинен чітко містити блоки Args та Returns, а також пояснювати специфіку асинхронної роботи або використання `sync_to_async`."
---
*Примітка: Усі згенеровані рішення були детально проаналізовані, протестовані локально в Docker-контейнері та модифіковані під поточні бізнес-вимоги додатків `shop_app`, `orders` та `users`.*